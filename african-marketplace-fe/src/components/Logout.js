import React { useEffect }from 'react';
import axiosWithAuth from './../utils/axiosWithAuth';

const Logout = (props) => {
    useEffect(()=> {
        axiosWithAuth()
        //.post
    })
    return (<div></div>)
}

export default Logout
